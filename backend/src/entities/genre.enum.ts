/**
 * Genre enum representing image categories in the OptiView system.
 * Uses string enum values for database readability.
 */
export enum Genre {
  NATURE = 'Nature',
  ARCHITECTURE = 'Architecture',
  PORTRAIT = 'Portrait',
  UNCATEGORIZED = 'Uncategorized',
}
